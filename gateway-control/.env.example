# Optional convenience default shown in the connection form.
# The API key is intentionally not configured here; users paste it in the browser UI.
VITE_DEFAULT_KVDB_BASE_URL=https://kvdb-api.example.com

